import * as Winston from "winston";
import {DIContainer} from "../di/DIContainer";
import {TypedConfiguration} from "../server/TypedConfiguration";

export class Log {
    // public static logger = new Winston.Logger(DIContainer.get(TypedConfiguration).logOptions);
}


